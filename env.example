# Example only (dummy values). Do NOT commit real keys.
# Copy env.example -> .env.local and replace the values with your real Firebase config.

NEXT_PUBLIC_FIREBASE_API_KEY=YOUR_FIREBASE_API_KEY
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=YOUR_PROJECT.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=YOUR_PROJECT_ID
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=YOUR_PROJECT.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=YOUR_SENDER_ID
NEXT_PUBLIC_FIREBASE_APP_ID=YOUR_APP_ID

# Cloud Functions (OpenRouter)
# Copy functions/env.example -> functions/.env and replace with your real key.
OPENROUTER_API_KEY=YOUR_OPENROUTER_API_KEY
OPENROUTER_MODEL=google/gemini-3-flash-preview

# Service Account JSON (for Cloud Functions - server-side only)
# DO NOT store service account JSON in source control.
# For local dev, prefer `GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/serviceAccount.json`
# or configure credentials in your deployment environment (Vercel/Cloud Functions/Secrets Manager).
